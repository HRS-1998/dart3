# FormItem 组件实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增 dart-form-item 组件（el-form-item 包装 + label 超长截断/tooltip），入库、playground 测试、docs 文档。

**Architecture:** 包装组件模式（同 common-btn/upload-file 入库模式）：`packages/dart3/packages/form-item/`（form-item.vue + index.ts withInstall）→ 注册入口 → playground demo + 路由 → docs examples/guide/sidebar。

**Tech Stack:** Vue 3（vue ^3.2.0，不用 defineModel/defineOptions）、Element Plus（el-form-item、el-tooltip、QuestionFilled 图标）。

**关键约束（执行者必读）：**

- **不执行任何 git commit；任务完成后由控制器统一 `git add`**（用户要求验证后再提交）
- 组件命名：`<script lang="ts">export default { name: 'DartFormItem', inheritAttrs: false }</script>` + `<script setup lang="ts">`
- 库内 utils：`import { withInstall } from '../utils'`
- playground/docs 的 vite alias 指向库源码 `packages/dart3/packages/index.ts`，改完即时生效
- 版本号：`packages/dart3/package.json` 2.1.12 → 2.1.12
- dev server（playground 5173 / docs 8081）已在运行，无需再启动；验证用 build 命令
- spec 见 `docs/superpowers/specs/2026-08-26-form-item-design.md`

---

### Task 1: FormItem 组件入库 + 注册 + 升版本

**Files:**

- Create: `packages/dart3/packages/form-item/form-item.vue`
- Create: `packages/dart3/packages/form-item/index.ts`
- Modify: `packages/dart3/packages/index.ts`（import / components 数组 / export 三处，位置：UploadFile 相关项之后）
- Modify: `packages/dart3/package.json`（version → 2.1.12）

form-item.vue 完整内容：

```vue
<template>
  <el-form-item v-bind="$attrs" :label="showLabel">
    <template v-if="isOverflow" #label>
      <span class="dart-form-item-label">
        {{ label.slice(0, maxLen) }}
        <el-tooltip :content="label" placement="top">
          <el-icon class="dart-form-item-more"><QuestionFilled /></el-icon>
        </el-tooltip>
      </span>
    </template>
    <template v-else-if="hasLabelSlot" #label
      ><slot name="label"></slot
    ></template>
    <template v-if="$slots.error" #error><slot name="error"></slot></template>
    <slot></slot>
  </el-form-item>
</template>

<script lang="ts">
export default {
  name: 'DartFormItem',
  inheritAttrs: false,
};
</script>

<script lang="ts" setup>
import { computed, useSlots } from 'vue';
import { QuestionFilled } from '@element-plus/icons-vue';

const props = withDefaults(
  defineProps<{
    label?: string;
    maxLen?: number; // label 最大字数，超过时截断并显示 tooltip 图标
  }>(),
  {
    label: '',
    maxLen: 6,
  },
);

const slots = useSlots();
const hasLabelSlot = computed(() => !!slots.label);

// 超长时改用 #label 结构渲染（截断文字 + tooltip 图标），此时字符串 label 置空
const isOverflow = computed(
  () => !hasLabelSlot.value && props.label.length > props.maxLen,
);
const showLabel = computed(() => (isOverflow.value ? '' : props.label));
</script>

<style lang="scss" scoped>
.dart-form-item-label {
  display: inline-flex;
  align-items: center;
}
.dart-form-item-more {
  margin-left: 2px;
  cursor: help;
  color: #909399;
}
</style>
```

说明：script setup 中 `label`/`maxLen` 可直接在模板使用（props 解构会失去响应性，故模板用 `label.slice(...)` 引用 props）。

index.ts 完整内容：

```ts
import { withInstall } from '../utils';
import formItem from './form-item.vue';

export const DartFormItem = withInstall(formItem);
export default DartFormItem;
```

`packages/index.ts` 三处（均在 UploadFile 之后）：

- `import FormItem from './form-item';`
- components 数组：`FormItem,`
- export 块：`FormItem,`

验证：`pnpm -F playground build` 成功。

### Task 2: Playground 测试页

**Files:**

- Create: `packages/playground/src/views/FormItemDemo.vue`
- Modify: `packages/playground/src/router.ts`（upload-file 路由后加 `/form-item`）

FormItemDemo.vue 完整内容：

```vue
<template>
  <div
    style="padding: 20px; display: flex; flex-direction: column; gap: 20px; max-width: 600px"
  >
    <el-form label-width="120px">
      <div>
        <h3>短 label（≤6 字，原样显示）</h3>
        <dart-form-item label="设备名称" prop="name">
          <el-input v-model="form.name" placeholder="短 label 无 icon" />
        </dart-form-item>
      </div>

      <div>
        <h3>长 label（>6 字，截断 + icon + tooltip）</h3>
        <dart-form-item label="所属公司组织机构编码" prop="orgCode">
          <el-input
            v-model="form.orgCode"
            placeholder="hover icon 看完整 label"
          />
        </dart-form-item>
      </div>

      <div>
        <h3>自定义 maxLen（4）</h3>
        <dart-form-item label="设备名称" :max-len="4" prop="type">
          <el-input v-model="form.type" placeholder="maxLen=4，4 字即截断" />
        </dart-form-item>
      </div>

      <div>
        <h3>#label 插槽（原样透传）</h3>
        <dart-form-item prop="remark">
          <template #label>
            <span style="color: #f56c6c">自定义label插槽</span>
          </template>
          <el-input v-model="form.remark" placeholder="插槽 label 不截断" />
        </dart-form-item>
      </div>
    </el-form>
  </div>
</template>

<script setup lang="ts">
import { reactive } from 'vue';

const form = reactive({ name: '', orgCode: '', type: '', remark: '' });
</script>
```

router.ts 追加：

```ts
  { path: '/form-item', name: 'FormItem', component: () => import('./views/FormItemDemo.vue') },
```

验证：`pnpm -F playground build` 成功。

### Task 3: Docs 文档

**Files:**

- Create: `packages/docs/examples/form-item/d1.vue`
- Create: `packages/docs/examples/form-item/d2.vue`
- Create: `packages/docs/guide/form-item.md`
- Modify: `packages/docs/.vitepress/config.ts`（sidebar 组件组，DatePicker 之后插入 `{ text: 'FormItem 表单项', link: '/guide/form-item' },`）

d1.vue：

```vue
<template>
  <el-form label-width="140px">
    <dart-form-item label="设备名称" prop="name">
      <el-input v-model="form.name" />
    </dart-form-item>
    <dart-form-item label="所属公司组织机构编码" prop="orgCode">
      <el-input v-model="form.orgCode" />
    </dart-form-item>
  </el-form>
</template>

<script lang="ts" setup>
import { reactive } from 'vue';

const form = reactive({ name: '', orgCode: '' });
</script>
```

d2.vue：

```vue
<template>
  <el-form label-width="140px">
    <dart-form-item label="设备名称" :max-len="4">
      <el-input v-model="form.name" placeholder="maxLen=4" />
    </dart-form-item>
    <dart-form-item>
      <template #label>
        <span style="color: #f56c6c">自定义label插槽</span>
      </template>
      <el-input v-model="form.remark" placeholder="插槽不截断" />
    </dart-form-item>
  </el-form>
</template>

<script lang="ts" setup>
import { reactive } from 'vue';

const form = reactive({ name: '', remark: '' });
</script>
```

guide/form-item.md：

```markdown
# FormItem 表单项

## 表单项介绍

描述：完全复用 el-form-item，label 超过最大字数（默认 6）时截断显示，并在其后展示问号图标，hover 显示完整 label
版本：2.1.12
功能：label 超长截断、tooltip 展示完整内容、最大字数可配置、#label 插槽透传

## 属性

| 属性     | 类型     | 默认值 | 描述                                        | 版本   |
| :------- | :------- | :----- | :------------------------------------------ | :----- |
| `label`  | `string` | `''`   | 表单项标签（超过 maxLen 时截断 + 问号图标） | 2.1.12 |
| `maxLen` | `number` | `6`    | label 最大字数                              | 2.1.12 |

其余属性（`prop`、`rules`、`required`、`label-width` 等）透传给 el-form-item。

## 插槽

| 插槽名称  | 描述                                     | 版本   |
| :-------- | :--------------------------------------- | :----- |
| `default` | 表单内容（透传）                         | 2.1.12 |
| `label`   | 自定义 label（传入后不做截断，原样透传） | 2.1.12 |
| `error`   | 自定义错误信息（透传）                   | 2.1.12 |

##### 使用示例

1: 默认使用（短 label 原样、长 label 截断 + tooltip）

<demo src="form-item/d1" />

2: 自定义 maxLen 与 #label 插槽

<demo src="form-item/d2" />
```

验证：`pnpm -F docs build` 成功。
